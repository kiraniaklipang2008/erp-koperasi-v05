
import { Transaksi } from "@/types";

/**
 * Initial empty transaction data - all Simpan, Pinjam, Angsuran data removed
 * Amount values set to null for dashboard chart reset
 */
export const initialTransaksi: Transaksi[] = [];
